{
  "name": "admissions-calculator",
  "private": true,
  "description": "Build and test tooling only; the site itself runs straight from these files.",
  "scripts": {
    "build": "node tools/build.js",
    "test": "node tools/run-tests.js"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
